{
  "compilerOptions": {
    "baseUrl": ".", // Adicione isso para garantir que o caminho base seja a raiz do projeto
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "exclude": ["node_modules", "dist"]
}